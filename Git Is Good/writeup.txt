압축 파일을 열어보면 flag.txt 파일과 .git 폴더가 존재한다.
당연히 flag 파일에 들어있는 문자열은 정답이 아니다.
git bash를 해당 경로에 열어서
git log -p를 입력하면 변경 로그를 볼 수 있다.
flag.txt 파일이 수정된 것을 확인할 수 있다.
git diff를 입력하여 파일의 내용이 어떻게 수정되었는지를 확인하면
flag를 획득할 수 있다.